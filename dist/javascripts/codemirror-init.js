const codeMirrorGoogle = CodeMirror.fromTextArea(document.getElementById('codemirror-google'), {
  mode: 'javascript',
});

const codeMirrorHorizon = CodeMirror.fromTextArea(document.getElementById('codemirror-horizon'), {
  mode: 'javascript',
});
